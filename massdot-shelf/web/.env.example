# Only needed to run the shelf standalone (`npm run dev`). The static export
# that ships inside the personal site is built by web/scripts/build-shelf.mjs,
# which sets these itself.
NEXT_PUBLIC_SANITY_PROJECT_ID=3scwu6mf
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SITE_URL=http://localhost:3000
